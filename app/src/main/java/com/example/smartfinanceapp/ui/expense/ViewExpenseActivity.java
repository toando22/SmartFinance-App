package com.example.smartfinanceapp.ui.expense;

import androidx.appcompat.app.AppCompatActivity;

// LỚP GIỮ CHỖ (Luật sẽ code nội dung sau)
public class ViewExpenseActivity extends AppCompatActivity {

    // LỚP NỘI (INNER CLASS) MÀ DATABASEHELPER CẦN
    // Cấu trúc hàm khởi tạo (constructor) PHẢI KHỚP với
    // hàm getExpensesByUserId của bạn (dòng 432)
    public static class Expense {
        public Expense(String expenseId, double amount, String categoryId, String description, String createAt) {
            // Thân hàm có thể để trống
            // Luật sẽ code nội dung sau
        }
    }
}

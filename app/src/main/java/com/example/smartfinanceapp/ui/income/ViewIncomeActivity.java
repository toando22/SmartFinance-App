package com.example.smartfinanceapp.ui.income;

import androidx.appcompat.app.AppCompatActivity;

// LỚP GIỮ CHỖ (Hiệp sẽ code nội dung sau)
public class ViewIncomeActivity extends AppCompatActivity {

    // LỚP NỘI (INNER CLASS) MÀ DATABASEHELPER CẦN
    // Cấu trúc hàm khởi tạo (constructor) PHẢI KHỚP với
    // hàm getIncomesByUserId của bạn (dòng 331)
    public static class Income {
        public Income(String income_id, double amount, String description, String create_at, String user_id, String category_id) {
            // Thân hàm có thể để trống
            // Hiệp sẽ code nội dung sau
        }
    }
}
